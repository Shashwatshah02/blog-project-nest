<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Blog</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to your CSS file -->
</head>
<body>
    <div class="container">
        <h1>View Blog</h1>

        <!-- Blog title -->
        <h2><%= blog.title %></h2>

        <!-- Blog image -->
        <% if (blog.image) { %>
            <img src="/public<%= blog.image %>" alt="Blog Image" style="max-width: 100%; height: auto;" /> <!-- Display image -->
        <% } else { %>
            <p><em>No image available</em></p> <!-- Message if no image -->
        <% } %>

        <!-- Category, Content, and Date -->
        <p><strong>Category:</strong> <%= blog.category ? blog.category.name : 'Uncategorized' %></p>
        <p><strong>Content:</strong></p>
        <p><%= blog.content %></p>
        <p><strong>Created At:</strong> <%= blog.createdAt.toISOString().split('T')[0] %></p> <!-- Format date -->

        <!-- Back to Blogs button -->
        <a href="/admin/blogs" class="button">Back to Blogs</a>
    </div>
</body>
</html>
